# Lightweight Face Recognition System Requirements
# Avoids heavy compilation dependencies like dlib

# Core Dependencies
opencv-python>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
Pillow>=10.0.0

# Web Framework
Flask>=2.3.0
Flask-Login>=0.6.0

# Data Processing
matplotlib>=3.7.0
tqdm>=4.65.0
pandas>=2.0.0

# Utility Libraries
typing-extensions>=4.7.0
imutils>=0.5.4

# Face Recognition without dlib (using OpenCV-based approach)
# We'll use OpenCV's DNN module and pre-trained models instead

# Optional Deep Learning (CPU version - lighter)
# torch>=2.0.0+cpu
# torchvision>=0.15.0+cpu

# Development
pytest>=7.4.0

# Production
gunicorn>=21.0.0
psutil>=5.9.0
